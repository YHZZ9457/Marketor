# Default: BaoStock first, optional Tushare second, local CSV fallback.
DATA_SOURCE=auto

# Optional. Only needed when explicitly using Tushare or enabling it as AUTO fallback.
# Never commit a real token or share it.
TUSHARE_TOKEN=
